.DS_Store
*.swp
*.swo
*~
*.bak
.env
.env.local
.claude/settings.local.json
.worktrees/

# Local MCP config holds live tokens — never commit it. Use .mcp.json.example as the template.
.mcp.json

# Internal Superpowers scratch (brainstorm state, server pid/log) — never ship
.superpowers/

# Distribution artifacts — the pip package (wxcc-toolkit) supersedes the zip
*.zip
/dist/
/build/
*.egg-info/
__pycache__/

# Internal working artifacts — not shipped in the pip package, not for GitHub
docs/superpowers/
docs/plans/
docs/tests/
docs/wxcc-pipeline-visual.html
/CODEX_PORT_PROMPT.md
/WxCC_AI_Agent_Builder_Toolkit.pptx

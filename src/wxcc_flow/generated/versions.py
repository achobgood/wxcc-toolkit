# GENERATED by tools/generator — do not edit. Regenerate instead.
"""Generated `wxcc-flow api versions` commands (Flow Store API)."""
import json
import typer
from wxcc_flow.client import FlowClient, FlowStoreError
from wxcc_flow.output import print_json, print_table

app = typer.Typer(help="Flow Store versions operations (generated).", no_args_is_help=True)


@app.command("list")
def cmd_list(
    flow_id: str = typer.Argument(..., help="flowId"),
    sort: str = typer.Option(None, "--sort", help=""),
    page: int = typer.Option(None, "--page", help="Fetch a single 0-based page. Omit to fetch all pages."),
    size: int = typer.Option(None, "--size", help="Page size (default 100 when fetching all)."),
    output: str = typer.Option("table", "-o", "--output", help="Output format: table|json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """getPublishedFlowVersions. [operationId: getPublishedFlowVersions]"""
    c = FlowClient(debug=debug)
    params = {}
    if sort is not None:
        params["sort"] = sort
    _path = f"/{c.org_id}/project/{c.project_id}/flows/{flow_id}/versions"
    try:
        if page is not None:
            params["page"] = page
            if size is not None:
                params["size"] = size
            data = c.get(_path, params=params)
            items = data if isinstance(data, list) else data.get("versions", data.get("items", data.get("data", [])))
        else:
            _size = size if size is not None else 100
            items = []
            _page = 0
            while True:
                _p = dict(params)
                _p["page"] = _page
                _p["size"] = _size
                data = c.get(_path, params=_p)
                batch = data if isinstance(data, list) else data.get("versions", data.get("items", data.get("data", [])))
                items.extend(batch)
                if len(batch) < _size:
                    break
                _page += 1
                if _page > 100000:
                    break
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    if output == "table":
        print_table(items, columns=[('Version ID', 'id'), ('Version', 'version'), ('Tag', 'tag'), ('Published', 'publishedDate'), ('Created By', 'createdBy')], limit=0)
    else:
        print_json(items)


@app.command("get")
def get(
    flow_id: str = typer.Argument(..., help="flowId"),
    version_id: str = typer.Argument(..., help="versionId"),
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """getFlowVersionById. [operationId: getFlowVersionById]"""
    c = FlowClient(debug=debug)
    params = {}
    _path = f"/{c.org_id}/project/{c.project_id}/flows/{flow_id}/versions/{version_id}"
    try:
        data = c.get(_path, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)


@app.command("latest")
def latest(
    flow_id: str = typer.Argument(..., help="flowId"),
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """getLatestFlowVersion. [operationId: getLatestFlowVersion]"""
    c = FlowClient(debug=debug)
    params = {}
    _path = f"/{c.org_id}/project/{c.project_id}/flows/{flow_id}/versions/latest"
    try:
        data = c.get(_path, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)


@app.command("draft")
def draft(
    flow_id: str = typer.Argument(..., help="flowId"),
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """getLatestFlowDraftVersion. [operationId: getLatestFlowDraftVersion]"""
    c = FlowClient(debug=debug)
    params = {}
    _path = f"/{c.org_id}/project/{c.project_id}/flows/{flow_id}/versions/draft"
    try:
        data = c.get(_path, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)


@app.command("all")
def cmd_all(
    search_by: str = typer.Option(None, "--search-by", help=""),
    sort: str = typer.Option(None, "--sort", help=""),
    page: int = typer.Option(None, "--page", help="Fetch a single 0-based page. Omit to fetch all pages."),
    size: int = typer.Option(None, "--size", help="Page size (default 100 when fetching all)."),
    output: str = typer.Option("table", "-o", "--output", help="Output format: table|json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """getAllFlowVersions. [operationId: getAllFlowVersions]"""
    c = FlowClient(debug=debug)
    params = {}
    if search_by is not None:
        params["searchBy"] = search_by
    if sort is not None:
        params["sort"] = sort
    _path = f"/{c.org_id}/project/{c.project_id}/flows/versions"
    try:
        if page is not None:
            params["page"] = page
            if size is not None:
                params["size"] = size
            data = c.get(_path, params=params)
            items = data if isinstance(data, list) else data.get("versions", data.get("items", data.get("data", [])))
        else:
            _size = size if size is not None else 100
            items = []
            _page = 0
            while True:
                _p = dict(params)
                _p["page"] = _page
                _p["size"] = _size
                data = c.get(_path, params=_p)
                batch = data if isinstance(data, list) else data.get("versions", data.get("items", data.get("data", [])))
                items.extend(batch)
                if len(batch) < _size:
                    break
                _page += 1
                if _page > 100000:
                    break
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    if output == "table":
        print_table(items, columns=[('Flow', 'flowId'), ('Version', 'version'), ('Tag', 'tag')], limit=0)
    else:
        print_json(items)


@app.command("all-latest")
def all_latest(
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """findAllLatestFlowVersions. [operationId: findAllLatestFlowVersions]"""
    typer.echo("Warning: 500s org-wide when never-published flows exist; fall back to 'api versions all'", err=True)
    c = FlowClient(debug=debug)
    params = {}
    _path = f"/{c.org_id}/project/{c.project_id}/flows/versions/all-latest"
    try:
        data = c.get(_path, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)

# GENERATED by tools/generator — do not edit. Regenerate instead.
"""Generated `wxcc-flow api util` commands (Flow Store API)."""
import json
import typer
from wxcc_flow.client import FlowClient, FlowStoreError
from wxcc_flow.output import print_json

app = typer.Typer(help="Flow Store util operations (generated).", no_args_is_help=True)


@app.command("test-expr")
def test_expr(
    json_body: str = typer.Option(None, "--json-body", help="Full JSON body (overrides flags)"),
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """testExpression. [operationId: testExpression]"""
    c = FlowClient(debug=debug)
    params = {}
    if json_body is not None:
        body = json.loads(json_body)
    else:
        body = {}
    _path = f"/expressionTest"
    try:
        data = c.post(_path, json_body=body, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)


@app.command("build-info")
def build_info(
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """buildInfo. [operationId: buildInfo]"""
    c = FlowClient(debug=debug)
    params = {}
    _path = f"/build_info"
    try:
        data = c.get(_path, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)


@app.command("health")
def health(
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """health. [operationId: health]"""
    c = FlowClient(debug=debug)
    params = {}
    _path = f"/health"
    try:
        data = c.get(_path, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)


@app.command("whoami")
def whoami(
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """getCurrentUser. [operationId: getCurrentUser]"""
    c = FlowClient(debug=debug)
    params = {}
    _path = f"/auth/user"
    try:
        data = c.get(_path, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)

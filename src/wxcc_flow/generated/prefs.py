# GENERATED by tools/generator — do not edit. Regenerate instead.
"""Generated `wxcc-flow api prefs` commands (Flow Store API)."""
import json
import typer
from wxcc_flow.client import FlowClient, FlowStoreError
from wxcc_flow.output import print_json

app = typer.Typer(help="Flow Store prefs operations (generated).", no_args_is_help=True)


@app.command("list")
def cmd_list(
    flow_id: str = typer.Argument(..., help="flowId"),
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """getFlowPreferences. [operationId: getFlowPreferences]"""
    c = FlowClient(debug=debug)
    params = {}
    _path = f"/{c.org_id}/project/{c.project_id}/flows/{flow_id}/preferences"
    try:
        data = c.get(_path, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)


@app.command("add")
def add(
    flow_id: str = typer.Argument(..., help="flowId"),
    empty: bool = typer.Option(None, "--empty/--no-empty", help=""),
    traversable_again: bool = typer.Option(None, "--traversable-again/--no-traversable-again", help=""),
    json_body: str = typer.Option(None, "--json-body", help="Full JSON body (overrides flags)"),
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """createFlowPreference. [operationId: createFlowPreference]"""
    c = FlowClient(debug=debug)
    params = {}
    if json_body is not None:
        body = json.loads(json_body)
    else:
        body = {}
        if empty is not None:
            body["empty"] = empty
        if traversable_again is not None:
            body["traversableAgain"] = traversable_again
    _path = f"/{c.org_id}/project/{c.project_id}/flows/{flow_id}/preferences"
    try:
        data = c.post(_path, json_body=body, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)


@app.command("set")
def cmd_set(
    flow_id: str = typer.Argument(..., help="flowId"),
    empty: bool = typer.Option(None, "--empty/--no-empty", help=""),
    traversable_again: bool = typer.Option(None, "--traversable-again/--no-traversable-again", help=""),
    json_body: str = typer.Option(None, "--json-body", help="Full JSON body (overrides flags)"),
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """updateFlowPreferences. [operationId: updateFlowPreferences]"""
    c = FlowClient(debug=debug)
    params = {}
    if json_body is not None:
        body = json.loads(json_body)
    else:
        body = {}
        if empty is not None:
            body["empty"] = empty
        if traversable_again is not None:
            body["traversableAgain"] = traversable_again
    _path = f"/{c.org_id}/project/{c.project_id}/flows/{flow_id}/preferences"
    try:
        data = c.put(_path, json_body=body, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)


@app.command("remove")
def remove(
    flow_id: str = typer.Argument(..., help="flowId"),
    empty: bool = typer.Option(None, "--empty/--no-empty", help=""),
    traversable_again: bool = typer.Option(None, "--traversable-again/--no-traversable-again", help=""),
    json_body: str = typer.Option(None, "--json-body", help="Full JSON body (overrides flags)"),
    force: bool = typer.Option(False, "--force", help="Skip the confirmation prompt"),
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """removeFlowPreferencesByPreferenceNames. [operationId: removeFlowPreferencesByPreferenceNames]"""
    if not force:
        typer.confirm(f"Delete {flow_id}?", abort=True)
    c = FlowClient(debug=debug)
    params = {}
    if json_body is not None:
        body = json.loads(json_body)
    else:
        body = {}
        if empty is not None:
            body["empty"] = empty
        if traversable_again is not None:
            body["traversableAgain"] = traversable_again
    _path = f"/{c.org_id}/project/{c.project_id}/flows/{flow_id}/preferences"
    try:
        data = c.delete_with_body(_path, json_body=body)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)


@app.command("tags")
def tags(
    flow_id: str = typer.Argument(..., help="flowId"),
    all_param: bool = typer.Option(None, "--all/--no-all", help="If true, returns all eligible tags. Otherwise returns tags attached to"),
    skip_rs_ep_check: bool = typer.Option(None, "--skip-rs-ep-check/--no-skip-rs-ep-check", help="If true, skips the check on connected Entry Points."),
    show_ext_vars: bool = typer.Option(None, "--show-ext-vars/--no-show-ext-vars", help="If true, returns the tags with externalized variables for the tag spec"),
    tag_id: str = typer.Option(None, "--tag-id", help="One of 'Live', 'Latest', 'Test', 'Dev'. The tag must be in use."),
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """List the tags of a flow. [operationId: getFlowTags]"""
    c = FlowClient(debug=debug)
    params = {}
    if all_param is not None:
        params["all"] = str(all_param).lower()
    if skip_rs_ep_check is not None:
        params["skipRsEPCheck"] = str(skip_rs_ep_check).lower()
    if show_ext_vars is not None:
        params["showExtVars"] = str(show_ext_vars).lower()
    if tag_id is not None:
        params["tagId"] = tag_id
    _path = f"/{c.org_id}/project/{c.project_id}/flows/{flow_id}/tags"
    try:
        data = c.get(_path, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)


@app.command("tag-history")
def tag_history(
    flow_id: str = typer.Argument(..., help="flowId"),
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """getTagHistories. [operationId: getTagHistories]"""
    c = FlowClient(debug=debug)
    params = {}
    _path = f"/{c.org_id}/project/{c.project_id}/flows/{flow_id}/tagHistories"
    try:
        data = c.get(_path, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)

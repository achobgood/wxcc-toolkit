# GENERATED by tools/generator — do not edit. Regenerate instead.
"""Generated `wxcc-flow api resource-collections` commands (Flow Store API)."""
import json
import typer
from wxcc_flow.client import FlowClient, FlowStoreError
from wxcc_flow.output import print_json

app = typer.Typer(help="Flow Store resource-collections operations (generated).", no_args_is_help=True)


@app.command("list")
def cmd_list(
    page: int = typer.Option(None, "--page", help="Defines the number of the displayed page. The page number starts from"),
    size: int = typer.Option(None, "--size", help="Defines the number of items to be displayed on a page."),
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """Get list of RCs for current user with simplified permission flags. [operationId: getResourceCollectionsByUserProfile]"""
    c = FlowClient(debug=debug)
    params = {}
    if page is not None:
        params["page"] = page
    if size is not None:
        params["size"] = size
    _path = f"/{c.org_id}/resource-collections"
    try:
        data = c.get(_path, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)

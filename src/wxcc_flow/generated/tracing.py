# GENERATED by tools/generator — do not edit. Regenerate instead.
"""Generated `wxcc-flow api tracing` commands (Flow Store API)."""
import json
import typer
from wxcc_flow.client import FlowClient, FlowStoreError
from wxcc_flow.output import print_json, print_table

app = typer.Typer(help="Flow Store tracing operations (generated).", no_args_is_help=True)


@app.command("interaction")
def interaction(
    flow_id: str = typer.Argument(..., help="flowId"),
    version_id: str = typer.Argument(..., help="versionId"),
    interaction_id: str = typer.Argument(..., help="interactionId"),
    convert_ids: bool = typer.Option(None, "--convert-ids/--no-convert-ids", help="Converts process IDs to diagram IDs. Possible values: 'true', 'false'"),
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """Get interaction metadata of a Flow Version object. [operationId: getInteraction]"""
    c = FlowClient(debug=debug)
    params = {}
    if convert_ids is not None:
        params["convertIds"] = str(convert_ids).lower()
    _path = f"/{c.org_id}/project/{c.project_id}/flows/{flow_id}/versions/{version_id}/interaction/{interaction_id}"
    try:
        data = c.get(_path, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)


@app.command("traces")
def traces(
    flow_id: str = typer.Argument(..., help="flowId"),
    version_id: str = typer.Argument(..., help="versionId"),
    interaction_id: str = typer.Argument(..., help="interactionId"),
    current_page: int = typer.Option(None, "--current-page", help="index of the current page for pagination"),
    page_size: int = typer.Option(None, "--page-size", help="determines the number of records in a page"),
    process_id: str = typer.Option(None, "--process-id", help="processId of the flow version"),
    doc_id: str = typer.Option(None, "--doc-id", help="document id of the flow trace"),
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """Get traces by interaction ID of a Flow Version object. [operationId: getTraces]"""
    c = FlowClient(debug=debug)
    params = {}
    if current_page is not None:
        params["currentPage"] = current_page
    if page_size is not None:
        params["pageSize"] = page_size
    if process_id is not None:
        params["processId"] = process_id
    if doc_id is not None:
        params["docId"] = doc_id
    _path = f"/{c.org_id}/project/{c.project_id}/flows/{flow_id}/versions/{version_id}/interaction/{interaction_id}/traces"
    try:
        data = c.get(_path, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)


@app.command("traces-decrypt")
def traces_decrypt(
    flow_id: str = typer.Argument(..., help="flowId"),
    version_id: str = typer.Argument(..., help="versionId"),
    interaction_id: str = typer.Argument(..., help="interactionId"),
    process_id: str = typer.Option(..., "--process-id", help="(required)processId of the flow version"),
    doc_id: str = typer.Option(None, "--doc-id", help="document id of the flow trace"),
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """Get traces by interaction ID of a Flow Version object. [operationId: getTracesDecrypt]"""
    c = FlowClient(debug=debug)
    params = {}
    if process_id is not None:
        params["processId"] = process_id
    if doc_id is not None:
        params["docId"] = doc_id
    _path = f"/{c.org_id}/project/{c.project_id}/flows/{flow_id}/versions/{version_id}/interaction/{interaction_id}/traces:decrypt"
    try:
        data = c.get(_path, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)


@app.command("analytics")
def analytics(
    flow_id: str = typer.Argument(..., help="flowId"),
    version_id: str = typer.Argument(..., help="versionId"),
    start_time: int = typer.Option(None, "--start-time", help="Epoch milliseconds for the start time of the period of interest. Examp"),
    end_time: int = typer.Option(None, "--end-time", help="Epoch milliseconds for the end time of the period of interest. Example"),
    output: str = typer.Option("json", "-o", "--output", help="Output format: json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """Get Analytics of a Flow Version object. [operationId: getAnalytics]"""
    c = FlowClient(debug=debug)
    params = {}
    if start_time is not None:
        params["startTime"] = start_time
    if end_time is not None:
        params["endTime"] = end_time
    _path = f"/{c.org_id}/project/{c.project_id}/flows/{flow_id}/versions/{version_id}/analytics"
    try:
        data = c.get(_path, params=params)
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    print_json(data)


@app.command("interactions")
def interactions(
    flow_id: str = typer.Argument(..., help="flowId"),
    from_param: int = typer.Option(None, "--from", help="Epoch milliseconds for the start time of the period of interest. Examp"),
    to: int = typer.Option(None, "--to", help="Epoch milliseconds for the end time of the period of interest. Example"),
    interaction_id: str = typer.Option(None, "--interaction-id", help="Interaction ID. In case only a certain interaction is needed. Example:"),
    tag_ids: str = typer.Option(None, "--tag-ids", help="Tags IDs. Comma-separated values of tag IDs"),
    version_id: str = typer.Option(None, "--version-id", help="Flow Version ID. Example: 64e6cddfb3abcc05186f5f06"),
    activity_diagram_id: str = typer.Option(None, "--activity-diagram-id", help="Activity Diagram ID. Use this if only interactions touching a specific"),
    search: str = typer.Option(None, "--search", help="search for ani, dnis, or interactionId"),
    sort_order: str = typer.Option(None, "--sort-order", help="sortOrder for the response, asc or desc"),
    page: int = typer.Option(None, "--page", help="Fetch a single 0-based page. Omit to fetch all pages."),
    size: int = typer.Option(None, "--size", help="Page size (default 100 when fetching all)."),
    output: str = typer.Option("json", "-o", "--output", help="Output format: table|json"),
    debug: bool = typer.Option(False, "--debug"),
):
    """Get all interactions of a flow across its versions. [operationId: getInteractions]"""
    c = FlowClient(debug=debug)
    params = {}
    if from_param is not None:
        params["from"] = from_param
    if to is not None:
        params["to"] = to
    if interaction_id is not None:
        params["interactionId"] = interaction_id
    if tag_ids is not None:
        params["tagIds"] = tag_ids
    if version_id is not None:
        params["versionId"] = version_id
    if activity_diagram_id is not None:
        params["activityDiagramId"] = activity_diagram_id
    if search is not None:
        params["search"] = search
    if sort_order is not None:
        params["sortOrder"] = sort_order
    _path = f"/{c.org_id}/project/{c.project_id}/flows/{flow_id}/interactions"
    try:
        if page is not None:
            params["currentPage"] = page
            if size is not None:
                params["pageSize"] = size
            data = c.get(_path, params=params)
            items = data if isinstance(data, list) else data.get("interactions", data.get("items", data.get("data", [])))
        else:
            _size = size if size is not None else 100
            items = []
            _page = 0
            while True:
                _p = dict(params)
                _p["currentPage"] = _page
                _p["pageSize"] = _size
                data = c.get(_path, params=_p)
                batch = data if isinstance(data, list) else data.get("interactions", data.get("items", data.get("data", [])))
                items.extend(batch)
                if len(batch) < _size:
                    break
                _page += 1
    except FlowStoreError as e:
        typer.echo(f"Error {e.status_code}: {e.body}", err=True)
        raise typer.Exit(1)
    if output == "table":
        print_table(items, columns=[("ID", "id"), ("Name", "name")], limit=0)
    else:
        print_json(items)
